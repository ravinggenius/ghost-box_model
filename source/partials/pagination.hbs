<nav class="pagination" role="pagination">
	{{#if next}}
		<a class="older-posts" href="{{page_url next}}">&larr;</a>
	{{/if}}

	<span class="page-number">Page {{page}} of {{pages}}</span>

	{{#if prev}}
		<a class="newer-posts" href="{{page_url prev}}">&rarr;</a>
	{{/if}}
</nav>
